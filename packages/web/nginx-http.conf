# Plain HTTP. Used when no certificate is mounted - see docker-entrypoint.sh.
#
# Fine for a laptop; not fine for anything a second person can reach. Mount a
# certificate and the entrypoint switches to nginx-tls.conf automatically.

server {
  listen 80;
  server_name _;

  include /etc/nginx/snippets/app.conf;
}
